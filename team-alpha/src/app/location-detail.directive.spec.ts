import { LocationDetailDirective } from './location-detail.directive';

describe('LocationDetailDirective', () => {
  it('should create an instance', () => {
    const directive = new LocationDetailDirective();
    expect(directive).toBeTruthy();
  });
});
